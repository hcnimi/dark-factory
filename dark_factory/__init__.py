"""Dark Factory: deterministic Python pipeline for spec-to-PR orchestration.

Migrates the /dark-factory slash command from LLM-interpreted markdown
to a code-controlled pipeline that calls the LLM only when intelligence
is required.
"""

__version__ = "0.1.0"
